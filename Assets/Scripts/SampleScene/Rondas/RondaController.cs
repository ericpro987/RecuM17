using UnityEngine;
[CreateAssetMenu(fileName = "RondaController", menuName = "Scriptable Objects/RondaController")]
public class RondaController : ScriptableObject
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public int rondaActual;
    public int enemicsActuals;
}
